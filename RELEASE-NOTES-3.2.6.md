# AV Matrix Studio 3.2.6

- Left-aligns permanent equipment-field titles above every input.
- Adds a review-first Excel import with row-by-row New, Merge, Duplicate,
  Ambiguous, Skipped, and Sheet Skipped outcomes.
- Reconciles every non-empty data row before enabling import.
- Shows exact blank fields and network interfaces that a merge will add.
- Leaves unchanged duplicates and ambiguous matches untouched.
- Expands supported spreadsheet header aliases and scans all worksheets.
- Keeps checkout ownership checks active so takeovers do not block sign-in.
- Preserves displaced checkout data locally when ownership changes.
- Accepts renamed packaged executables in the verified self-updater.
